[phases.build]
cmds = ["pip install -r requirements.txt"]

[phases.start]
cmd = "uvicorn app.api:app --host 0.0.0.0 --port $PORT"

[static]
# Create necessary directories
dirs = ["data", "logs", "models", "static"]

[environment]
PYTHONPATH = "/app"
PYTHONUNBUFFERED = "1"